const config = {
    "userStoreKey": "user",
    "settingStoreKey": "setting",
    "toastOptions": {
        position: "top-center",
        theme: "dark",
        autoClose: 1200,
    }
};

export default config;
