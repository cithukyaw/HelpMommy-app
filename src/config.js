const config = {
    "exchangeRate": 100, // MMK per 1 heart
    "currencyUnit": "Ks",
    "userStoreKey": "user",
    "toastOptions": {
        position: "top-center",
        theme: "dark",
    }
};

export default config;
