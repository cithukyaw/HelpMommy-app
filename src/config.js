const config = {
    "exchangeRate": 50, // MMK per 1 heart
    "currencyUnit": "Ks",
    "userStoreKey": "user",
    "toastOptions": {
        position: "top-center",
        theme: "dark",
        autoClose: 1000,
    }
};

export default config;
